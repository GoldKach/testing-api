# version: '3.8'

# services:
#   api:
#     build: .
#     container_name: myapi
#     restart: unless-stopped
#     ports:
#       - "8000:8000"
#     env_file:
#       - .env
#     networks:
#       - app-network

# networks:
#   app-network:
#     driver: bridge


version: '3.8'

services:
  api:
    build: .
    container_name: myapi
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge